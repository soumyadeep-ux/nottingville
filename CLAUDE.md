# Nottingville — Girls' Hostel Website

Single static HTML page. No framework, no build step. Just index.html + images/.

## Deploy
**NEVER deploy `.` directly** — MP4/video files in the repo root break Cloudflare Pages (emoji filenames cause 404s for all URLs with query params like `?fbclid=`).

```bash
# Clean deploy: copy only site files to temp dir, deploy that
# NOTE: `cp *.html` now picks up index.html AND the 8 landing pages.
# _headers and assets/ MUST be copied or caching + LP styling silently break.
mkdir -p /tmp/nottingville-deploy/images /tmp/nottingville-deploy/assets && \
  cp *.html robots.txt sitemap.xml _headers favicon.svg favicon-32x32.png apple-touch-icon.png /tmp/nottingville-deploy/ && \
  cp assets/lp.css assets/lp.js /tmp/nottingville-deploy/assets/ && \
  cp images/hostel-*.jpg images/hostel-*.webp /tmp/nottingville-deploy/images/ 2>/dev/null; \
  npx wrangler pages deploy /tmp/nottingville-deploy --project-name nottingville --commit-dirty=true && \
  rm -rf /tmp/nottingville-deploy

git push origin main
```
Wrangler OAuth already configured. If it expires: `npx wrangler login`

## Google Ads landing pages (added 2026-07-22)

8 LPs generated by `build-lp.py` from one config block each. Run
`python3 build-lp.py` after editing a config; it rewrites the `.html` files and
`sitemap.xml`. The deployed site stays plain static — there is no deploy-time build.

| Page | Ad group / cluster | Evidence at build time |
|---|---|---|
| `/pg-durgapur` | PG Durgapur | 216 clicks, 8 conv (best ad group) |
| `/girls-hostel-durgapur` | Hostel Durgapur | 97 clicks, 3 conv |
| `/room-rent-durgapur` | Room Rent Durgapur | 181 clicks, 5 conv |
| `/pg-city-centre-durgapur` | City Centre + Bidhannagar | **66 clicks, 6 conv, 9.1% CVR — best in account** |
| `/durgapur-hostel-fees` | Fees / price intent | historical 25% CVR on "durgapur hostel fees" |
| `/single-room-pg-durgapur` | Single / private room | 10 clicks, 9 terms, unowned |
| `/hostel-near-coaching-durgapur` | Coaching Proximity | 10 clicks, 0 conv — a bet on highest-LTV audience |
| `/locations` | Benachity / Sepco / Fuljhore long tail | serves it honestly; we have no property there |

**Positioning: food first, on every page.** Verified facts only: 4 meals daily,
non-veg 6 days a week, included in rent, "Home like Food" is a real Google review
quote. Rating 4.6 from 17 Google reviews is displayed but deliberately NOT marked
up as `aggregateRating` — those reviews live on Google, and self-marking
third-party reviews breaches Google's structured-data policy.

**LPs do NOT use the lead-capture modal.** `index.html` intercepts every WhatsApp
click and forces a 7-field form; that gate is why WhatsApp logged 6 conversions in
90 days against 29 on-page entry points, while ungated `tel:` clicks logged 16.
`assets/lp.js` lets WhatsApp links open directly and bakes the qualifying context
into a per-page prefilled message instead. The enquiry is still logged to the
Google Sheet via a non-blocking pixel.

**Open items for the owners:** exact AC / Non-AC / sharing tariff (the table has a
marked slot; deliberately not guessed), and whether a mess-only service is sold
(5 mess keywords are live with no page).

## Stack
- `index.html` — entire site (HTML + CSS + JS inline)
- `build-lp.py` + `assets/lp.css` + `assets/lp.js` — the landing-page system
- `_headers` — Cloudflare cache rules (site was serving everything uncached)
- `images/hostel-1.jpg`, `hostel-2.jpg`, `hostel-3.jpg` — hero + story photos
- Google Fonts: Cormorant Garamond (headings) + DM Sans (body)
- Cloudflare Pages (project: `nottingville`) + Domain: `nottingville.space`
- GitHub: github.com/soumyadeep-ux/nottingville

## Hostel Facts (source of truth)
- City: **DURGAPUR** — never say Kolkata
- Run by 3 mothers, for daughters (core identity)
- 60 beds (AC + Non-AC), expanding to 100
- 4 meals/day: breakfast, lunch, evening snacks+tea, dinner
- Curfew: gate closes 7 PM, opens 6 AM
- 24x7 CCTV, wardens, daily attendance register, medical support
- Locations: 10/1 Sarojini Naidu Path, 10/3 Sarojini Naidu Path, D45 Uday Shankar, 55 Tarashankar

## Contact Numbers
- +91 91266 60502 (primary, WhatsApp)
- +91 79089 78959 (Durgapur)
- +91 84361 50885
- WhatsApp link uses: `919126660502`

## Target Audience
- Primary: Students preparing for NEET UG, JEE Main, JEE Advanced, BITSAT, CUET
- Secondary: Parents of college girls
- Nearby coaching: Adhyayan Durgapur, Physics Wallah, Aakash Institute, Allen Career Institute

## Lead Capture
- Google Sheet "Nottingville Leads" (ID: `1dtCbSjtWOk5kW02XS7SshApDiE6tSM9dJb4zHoH0WcI`)
- Apps Script: `apps-script-leads.js` — doGet + doPost → writes to "Leads" tab
- Browser fires GET image pixel to Apps Script (POST/sendBeacon don't survive Apps Script 302 redirects)
- `window.__SHEETS_ENDPOINT` in index.html holds the deployed Apps Script URL
- To redeploy script: paste `apps-script-leads.js` into Apps Script editor → Deploy → New deployment → Web app → Anyone

## Design System
- `--ochre: #C8800A` | `--terra: #B83E2C` | `--cream: #FAF6EE` | `--deep: #231008`
- Hero bg: `grayscale(100%) brightness(0.65)` for text legibility
- CRO decision: NO forms — WhatsApp tap-to-chat only (lowest friction)
- Font weights: body 400, headings Cormorant Garamond 400-600

## Google Ads
- **Account ID:** `5886623748` (under MCC `8952449784`)
- **Google Ads Tag:** `AW-11090833551` (in `index.html` `<head>`)
- **Conversion tracking:** WhatsApp click + Phone call — both fire via JS event delegation (lines 1210-1227), NOT inline `onclick`
- **Strategy doc:** `google-ads-strategy.md` — full campaign structure, keywords, ad copy, baselines
- **Weekly check-in:** `/google-ads-weekly` skill — automated performance audit
- **MCP server:** `/Users/soumyadeepmukherjee/Documents/mcp-google-ads/google_ads_server.py` — read + write tools
- **MCP write tools:** `create_campaign_budget`, `create_search_campaign`, `create_ad_group`, `add_keywords`, `add_negative_keywords`, `create_responsive_search_ad`, `update_entity_status`, `update_budget_amount`, `add_geo_targets`, `search_geo_targets`, `add_ad_schedule`, `add_call_asset`, `add_sitelink_asset`, `add_callout_asset`, `remove_entity`
- **Live campaigns (deployed 2026-03-21):**
  - "Nottingville — Durgapur City" (ID 23677229903) — Durgapur city, INR 100/day, Manual CPC
  - "Nottingville — Near Durgapur" (ID 23671816026) — 40km radius excl. city, INR 100/day, Manual CPC
  - 4 ad groups each: PG Durgapur, Hostel Durgapur, Coaching Proximity, Room Rent Durgapur
- **Key insight:** Durgapur-specific keywords convert (3-6% CVR), "near me" keywords waste budget (0% CVR). Always use phrase match with city name.
- **Prices start at INR 6,000/month** — ads reflect this; budget searchers (under 3000) are negated
- **Old campaign** "EK | Theme | Pg / Hostel in Durgapur" (ID 19668768710) — paused, kept for historical data reference

## Gotchas
- **CRITICAL: Never `wrangler pages deploy .`** — MP4 files with emoji filenames in repo root corrupt the deployment. All URLs with query params (`?fbclid=`, `?utm_source=`) return 404. Site appears to work because Cloudflare edge cache serves stale root `/` but ad traffic (which always has `?fbclid=`) breaks. Use the clean deploy command above.
- Domain was WHOIS-suspended on first deploy — always verify registrant email before DNS changes
- Cloudflare Pages UI GitHub connect had redirect loop bug — use wrangler CLI instead
- Hero text cut off on mobile — fixed via `padding-top: 5rem` on `.hero`
- Wrangler: create project first if new: `npx wrangler pages project create nottingville --production-branch main`
- gh CLI authenticated as `soumyadeep-ux` (keyring, no token needed)
- Cloudflare DNS nameservers: `ainsley.ns.cloudflare.com`, `kobe.ns.cloudflare.com`
